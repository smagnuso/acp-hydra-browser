// Injected by scripts/build-ui.mjs via esbuild's `define` at bundle
// time. Ambient (no imports/exports in this file) so it is a real global
// rather than a module-scoped declaration.
declare const __BUILD_ID__: string;
declare const __APP_VERSION__: string;
